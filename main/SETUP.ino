
void SETUP(){
            pinMode(RED, INPUT);
            pinMode(RED, OUTPUT);
            pinMode(AMBER, INPUT);
            pinMode(AMBER, OUTPUT);
            pinMode(GREEN, INPUT);
            pinMode(GREEN, OUTPUT);
}
